# Added by ken

### To run

```
docker compose up --build
```

> Do this inside directory with the compose file
> It composes and automatically builds the images needed

# Structure

Dataset(data/), Dashboarding(dashboard/), Scripts(scripts/)

- Inside /opt/airflow/plugins

Dags(workflows/dags/)

- Inside /opt/airflow/dags
